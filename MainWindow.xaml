<Window x:Class="ExeLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
    Title="ExeLauncher" Height="420" Width="760"
    AllowDrop="True" PreviewDragOver="Window_PreviewDragOver" Drop="Window_Drop">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Simulated desktop / workspace and actions -->
        <StackPanel Grid.Column="0" Margin="8">
            <TextBlock Text="Simulated Desktop" FontSize="18" FontWeight="Bold" />
            <TextBlock Text="Drag files here to add to the workspace." FontStyle="Italic" FontSize="12" Margin="0,4,0,8" />

            <Border BorderBrush="LightGray" BorderThickness="1" Padding="8" Height="240">
                <ScrollViewer>
                    <ItemsControl ItemsSource="{Binding WorkspaceItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="140" Height="60" Margin="6" Command="{Binding DataContext.RunCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding FullPath}" Click="WorkspaceItem_Click">
                                    <StackPanel>
                                        <TextBlock Text="{Binding FileName}" TextWrapping="Wrap" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox Width="340" Text="{Binding SelectedPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Browse" Margin="8,0,0,0" Width="80" Command="{Binding BrowseCommand}" />
            </StackPanel>
            <Button Content="Run File" Margin="0,12,0,0" Width="120" Height="36" Command="{Binding RunCommand}" />

            <TextBlock Text="Recent Files" Margin="0,16,0,0" FontWeight="SemiBold" />
            <ListBox ItemsSource="{Binding RecentFiles}" SelectedItem="{Binding SelectedPath}" Height="80" />
        </StackPanel>

        <!-- Right: Settings panel -->
        <StackPanel Grid.Column="1" Margin="8">
            <TextBlock Text="Settings" FontSize="16" FontWeight="Bold" />
            <StackPanel Orientation="Vertical" Margin="0,8,0,0">
                <TextBlock Text="Theme" />
                <ComboBox SelectedValue="{Binding Settings.Theme}" Width="120">
                    <ComboBoxItem>Light</ComboBoxItem>
                    <ComboBoxItem>Dark</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="Default Extraction Folder" Margin="0,8,0,0" />
                <TextBox Text="{Binding Settings.DefaultExtractionFolder}" Width="220" />
                <Button Content="Save Settings" Margin="0,12,0,0" Width="120" Command="{Binding SaveSettingsCommand}" />
            </StackPanel>
            <TextBlock Text="Warnings" Margin="0,24,0,0" FontWeight="SemiBold" />
            <TextBlock Text="The app will never auto-run files. Click 'Run File' to execute." TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
